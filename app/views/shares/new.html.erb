<div class="container">
    
  <h1>投稿新規作成ページ</h1>
  
  <%= link_to shares_path, method: :get do %>
    <i class="btn btn-secondary mb-3">一覧に戻る</i>
  <% end %>
  

  <%= render 'form', share: @share %>

</div>
  