<%= form_with(model: share, local: true) do |f| %>

  <%= f.label :title, 'タイトル' %>
  <%= f.text_field :title %>
  
  <%= f.label :content, '内容' %>
  <%= f.text_field :content %>

  <%= f.label :place, '場所' %>
  <%= f.text_field :place %>

  <%= f.submit '投稿' %>
<% end %>